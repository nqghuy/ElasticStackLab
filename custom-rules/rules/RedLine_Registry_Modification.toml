[metadata]
creation_date = "2026/09/06"
integration = ["endpoint", "windows"]
maturity = "production"
updated_date = "2026/09/06"

[rule]
name = "RedLine Registry Modification"
description = "RedLine Registry Modification"
risk_score = 21
severity = "low"
license = ""
interval = "5m"
from = "now-360s"
to = "now"
max_signals = 100

rule_id = "05a5e2b3-f14a-4084-8612-4b9734f79ee9"

author = ["Huy"]

tags = [
    "OS: Windows"
]


false_positives = []

exceptions_list = []

required_fields = [
    { name = "event.type", type = "keyword", ecs = true },
    { name = "host.os.type", type = "keyword", ecs = true },
    { name = "process.executable", type = "keyword", ecs = true },
    { name = "process.name", type = "keyword", ecs = true },
    { name = "registry.data.strings", type = "keyword", ecs = true },
    { name = "registry.hive", type = "keyword", ecs = true },
    { name = "registry.path", type = "keyword", ecs = true },
    { name = "registry.value", type = "keyword", ecs = true },
    { name = "user.id", type = "keyword", ecs = true },
]

related_integrations = []

type = "eql"
language = "eql"

index = [
    "logs-windows.sysmon_operational*",
    "logs-endpoint.events.process-*",
    "logs-endpoint.events.registry-*",
    "winlogbeat-*",
    "logs-windows.powershell_operational*"
]

query = '''
any where host.os.type == "windows" and  
(
  process.name: "reg.exe" and  process.parent.name : (
    "powershell.exe",
    "pwsh.exe",
    "cmd.exe",
    "wscript.exe",
    "cscript.exe",
    "mshta.exe",
    "rundll32.exe",
    "regsvr32.exe",
    "wmic.exe"
  ) or 
  process.command_line: ("*New-ItemProperty*", "*New-Item*", "*Set-ItemProperty*")
) and 
(
  (
    process.command_line: ("*SOFTWARE\\Policies\\Microsoft\\Windows\\WindowsUpdate*") and
    (
      process.command_line: ("*-Value*1*", "*/d*1*") and
      process.command_line: (
        "*NoAutoUpdate*",
        "*NoAutoRebootWithLoggedOnUsers*",
        "*DoNotConnectToWindowsUpdateInternetLocations*"
      )
    ) or
    (
      process.command_line: ("*-Value*2*", "*/d*2*") and
      process.command_line: "*AUOptions*"
    )
  ) or 
  (
    process.command_line: "*SOFTWARE\\Microsoft\\Windows Defender\\Features*" and
    process.command_line: "*TamperProtection*"
  )
)

'''

filters = []
actions = []

setup = ""

[[rule.threat]]
framework = "MITRE ATT&CK"

[[rule.threat.technique]]
id = "T1112"
name = "Modify Registry"
reference = "https://attack.mitre.org/techniques/T1112/"

[rule.threat.tactic]
id = "TA0112"
name = "Defense Impairment"
reference = "https://attack.mitre.org/tactics/TA0112/"
