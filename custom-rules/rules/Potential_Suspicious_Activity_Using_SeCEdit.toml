[metadata]
creation_date = "2026/08/26"
integration = ["endpoint", "windows"]
maturity = "production"
updated_date = "2026/08/26"

[rule]
name = "Potential Suspicious Activity Using SeCEdit"
description = "Detects potential suspicious behaviour using secedit.exe. Such as exporting or modifying the security policy"
risk_score = 21
severity = "low"
license = ""
interval = "5m"
from = "now-360s"
to = "now"
max_signals = 100

rule_id = "1d2745de-b21d-4852-ac9f-218dd9a2b1c1"

author = ["Huy"]

tags = [
    "OS: Windows"
]


false_positives = []

exceptions_list = []

required_fields = [
    { name = "event.type", type = "keyword", ecs = true },
    { name = "host.os.type", type = "keyword", ecs = true },
    { name = "process.executable", type = "keyword", ecs = true },
    { name = "process.name", type = "keyword", ecs = true },
    { name = "process.args", type = "keyword", ecs = true },
    { name = "process.parent.name", type = "keyword", ecs = true },
]

related_integrations = []

type = "eql"
language = "eql"

index = [
    "logs-windows.sysmon_operational*",
    "logs-endpoint.events.process-*",
    "logs-endpoint.events.registry-*",
    "winlogbeat-*",
    "logs-windows.powershell_operational*",
    "logs-system.security*"
]

query = '''
process where host.os.type == "windows" and
event.code in ("1", "4688") and 
process.name: "secedit.exe" and 
(process.command_line: "*/configure*" and process.command_line: "*/db*") or 
(process.command_line: "*/import*" and process.command_line: "*/cfg")
'''

filters = []
actions = []

setup = ""

[[rule.threat]]
framework = "MITRE ATT&CK"

[[rule.threat.technique]]
id = "T1547"
name = "Boot or Logon Autostart Execution"
reference = "https://attack.mitre.org/techniques/T1547/"

[[rule.threat.technique.subtechnique]]
id = "T1547.001"
name = "Registry Run Keys / Startup Folder"
reference = "https://attack.mitre.org/techniques/T1547/001/"

[rule.threat.tactic]
id = "TA0003"
name = "Persistence"
reference = "https://attack.mitre.org/tactics/TA0003/"

